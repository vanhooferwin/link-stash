# Bookmark Dashboard Environment Configuration
# Copy this file to .env and update the values

# Required: Session secret for secure cookies
# Generate with: openssl rand -base64 32
SESSION_SECRET=your-secure-random-string-here

# Optional: Port to expose the application (default: 3005)
PORT=3005

# For GHCR deployment (docker-compose.ghcr.yml)
# Your GitHub username for pulling images from ghcr.io
GITHUB_USER=your-github-username

# Image tag to use (default: latest)
IMAGE_TAG=latest
